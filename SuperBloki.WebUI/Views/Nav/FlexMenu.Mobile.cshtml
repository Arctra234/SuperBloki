@model IEnumerable<string>

<div data-role="navbar">
    <ul>
        @foreach (var link in Model)
        {
            <li>
                @Html.RouteLink(link, new
           {
               controller = "Constructor",
               action = "List",
               producer = link,
               page = 1
           }, new
           {
               data_transition = "fade",
               @class = (link == ViewBag.SelectedProducer ? "ui-btn-active" : null)
           })
            </li>
        }
    </ul>
</div>